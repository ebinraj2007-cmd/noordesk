#!/bin/bash
# Double-click this file in Finder to start NoorDesk.
# (First time: right-click -> Open, then click "Open" to get past macOS Gatekeeper.)
cd "$(dirname "$0")"
echo "Starting NoorDesk…"
bash start.sh

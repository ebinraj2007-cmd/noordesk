fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
lingua-language-detector>=2.0,<3.0
anthropic>=0.40           # optional: only needed if you enable the AI engine
pytest>=8.0               # dev/test only
httpx>=0.27               # dev/test only (FastAPI TestClient)
